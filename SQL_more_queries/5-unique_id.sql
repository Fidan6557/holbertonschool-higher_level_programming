-- a script that creates the table unique_id
-- a script that creates the table unique_id
CREATE TABLE IF NOT EXISTS unique_id (id INT NOT NULL, 
                                    name VARCHAR(256));